/** Identifier reported by the public `GET /api-info` endpoint. */
export const API_INFO_SERVICE_NAME = 'directory-service';

/** Used when `NODE_ENV` is not set in the running environment. */
export const DEFAULT_ENVIRONMENT = 'development';

/** Used when the version cannot be resolved from a `package.json`. */
export const UNKNOWN_VERSION = 'unknown';
